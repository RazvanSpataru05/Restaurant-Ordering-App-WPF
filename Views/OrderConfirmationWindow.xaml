<Window x:Class="RestaurantOrderingApp.Views.OrderConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RestaurantOrderingApp.Views"
        mc:Ignorable="d"
        Title="" Height="220" Width="400"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        WindowStyle="None"
        Background="#1C1C1C"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="HeaderButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#C9A84C"/>
            <Setter Property="BorderBrush" Value="#C9A84C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="18 8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Georgia"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#C9A84C"/>
                                <Setter Property="Foreground" Value="#1C1C1C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border BorderBrush="#C9A84C" BorderThickness="1" Padding="30 20">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="SUCCESS!" FontSize="30"
                       Foreground="#C9A84C"
                       FontFamily="Georgia"
                       FontStyle="Italic"
                       HorizontalAlignment="Center"
                       Margin="0 5 0 0"/>
            <TextBlock Text="✔" FontSize="24"
                       Foreground="#C9A84C"
                       HorizontalAlignment="Center"
                       Margin="0 0 0 15"/>
            <TextBlock TextWrapping="Wrap" HorizontalAlignment="Center" 
                        TextAlignment="Center" Margin="0,0,0,25">
                        <Run Text="Your order with code " Foreground="#E0E0E0" FontFamily="Georgia" FontSize="12"/>
                        <Run Text="{Binding OrderCode}" Foreground="#C9A84C" FontFamily="Consolas" FontSize="12"/>
                        <Run Text=" has been placed successfully." Foreground="#E0E0E0" FontFamily="Georgia" FontSize="12"/>
                        <LineBreak/>
                        <Run Text="Estimated time of delivery: " Foreground="#E0E0E0" FontFamily="Georgia" FontSize="12"/>
                        <Run Text="{Binding EstimatedDelivery}" Foreground="#C9A84C" FontFamily="Consolas" FontSize="12"/>
            </TextBlock>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Close"
                        Command="{Binding CloseCommand}"
                        Style="{StaticResource HeaderButtonStyle}"
                        Width="75"
                        Margin="0 0 0 10"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
